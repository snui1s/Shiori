---
import Layout from "../layouts/Layout.astro";
---

<Layout title="404 - you lost | Shiori 栞">
  <div class="container min-h-[80vh] flex items-center justify-center p-4">
    <div
      class="max-w-[600px] w-full p-8 md:p-16 text-center rounded-[2.5rem] glass animate-fade-in relative overflow-hidden"
    >
      <div class="mb-8 flex justify-center">
        <div
          class="lantern-404 w-20 h-[100px] flex flex-col items-center drop-shadow-[0_0_20px_rgba(246,48,73,0.4)] animate-swing"
        >
          <div class="w-10 h-2.5 bg-[#1a1a1a] rounded-sm"></div>
          <div
            class="flex-1 w-[70px] bg-gradient-to-r from-primary via-[#cc233b] to-primary rounded-[20px] relative flex flex-col justify-evenly border-2 border-black/20 overflow-hidden"
          >
            <div class="w-full h-0.5 bg-black/30"></div>
            <div class="w-full h-0.5 bg-black/30"></div>
            <div
              class="absolute inset-0 flex items-center justify-center text-black/40 font-extrabold text-[1.2rem] pointer-events-none"
            >
              404
            </div>
          </div>
          <div class="w-10 h-2.5 bg-[#1a1a1a] rounded-sm"></div>
        </div>
      </div>

      <h1 class="text-[2rem] md:text-[2.5rem] font-bold mb-6 h-gradient">
        โอ๊ะ! หลงทางซะแล้ว
      </h1>
      <p class="text-text-muted text-lg md:text-[1.1rem] leading-relaxed mb-12">
        ดูเหมือนสิ่งที่คุณตามหาจะไม่ได้อยู่ที่นี่
        บางทีมันอาจจะยังไม่ถูกเขียนขึ้น หรือถูกผมลบไปแล้ว...
      </p>

      <div class="flex flex-col md:flex-row gap-4 justify-center">
        <a
          href="/"
          class="group bg-primary text-white py-4 px-8 rounded-full font-bold flex items-center justify-center gap-3 transition-all duration-300 shadow-[0_10px_20px_rgba(246,48,73,0.3)] hover:-translate-y-1 hover:shadow-[0_15px_30px_rgba(246,48,73,0.5)] no-underline"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            ><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"
            ></path><polyline points="9 22 9 12 15 12 15 22"></polyline></svg
          >
          กลับหน้าหลัก
        </a>
        <a
          href="/blog"
          class="py-4 px-8 border border-white/10 text-white rounded-full font-semibold transition-all duration-300 hover:bg-white/5 hover:border-white no-underline"
          >ไปดูบันทึกอื่นๆ</a
        >
      </div>
    </div>
  </div>
</Layout>

<style>
  @keyframes swing {
    from {
      transform: rotate(-10deg);
    }
    to {
      transform: rotate(10deg);
    }
  }
  .animate-swing {
    animation: swing 3s ease-in-out infinite alternate;
  }
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in {
    animation: fade-in 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }
</style>
